# CLAUDE.md - Realtime Game Backend 协作说明

## 最高优先级规则

1. 开始任何工作前，先阅读本文件、`docs/README.md`、当前任务和相关 ADR。
2. 未经用户确认，不要修改项目方向、技术栈、服务边界或阶段范围。
3. 每次只处理一个任务、一个分支和一个明确验收目标。
4. 编码前先输出理解、计划、涉及文件、风险和测试方案，等待用户确认。
5. 不擅自增加功能，不提前引入 Kafka、etcd、多节点或 Kubernetes。
6. 不以“技术先进”为理由增加组件；每次迭代必须对应真实问题或验收标准。
7. 不编造测试结果、性能数字、线上数据或项目规模。
8. 不把密钥、Token、SSH 私钥、数据库密码写入仓库或聊天记录。
9. 任务结束后必须给出改动说明、测试结果、遗留风险和下一步建议。
10. 用户始终拥有最终合并权和需求变更权。

## 项目身份

这是一个分布式实时对战服务后端，不是通用 RPC 框架，也不是完整游戏客户端。

本项目重点验证：

- 主流 C++ 后端技术栈的实际使用能力。
- 服务边界、数据所有权和接口契约设计。
- 有状态服务的并发、持久化和恢复。
- 幂等、故障处理、背压和可观测性。
- Docker 化开发、测试、压测、故障注入和工程文档。

## 固定技术方向

- 开发环境：WSL2 Ubuntu 26.04 LTS + CLion Remote Toolchain
- 语言与构建：C++20、GCC 15.2、CMake 4.2.3、Ninja 1.13.2、vcpkg
- 服务通信：brpc + Protobuf
- 浏览器通信：HTTP + WebSocket
- 数据组件：Redis、MySQL、Kafka、etcd
- Web 演示：Vue 3、TypeScript、Vite、Canvas
- 可观测性：Prometheus、Grafana、OpenTelemetry
- 测试：GoogleTest、ASan、TSan、UBSan
- 集成环境：Docker Compose、GitHub Actions

除 ADR 批准外，不使用第二套 RPC 框架，不更换 C++ 技术栈。

## 服务边界

| 服务 | 职责 | 不应承担 |
|---|---|---|
| Gateway | HTTP/WebSocket、鉴权、路由、心跳、限流 | 匹配策略、房间状态权威计算 |
| Match | 匹配队列、分差放宽、取消、房间分配 | 保存战斗状态、直接推送客户端 |
| Room/Battle | 房间生命周期、权威状态、快照、帧数据 | 玩家长期档案、跨服务结算逻辑 |
| Player/State | 玩家、会话、战绩、排行榜读取 | 实时战斗循环 |
| Settlement | 消费事件、幂等结算、统计 | 承接客户端同步请求 |

服务边界详细定义见 `docs/01-architecture.md`。修改边界必须先新增或更新 ADR。

## 数据所有权

- Redis 用于短期状态、缓存、会话、排行和分布式协调，不作为唯一真相。
- MySQL 用于需要恢复的玩家、房间记录和战绩。
- Kafka 保存可回放的领域事件，不作为同步请求数据库。
- 每个服务只直接修改自己拥有的数据，跨服务数据通过接口或事件协作。
- 所有可能重试的写操作必须定义幂等键和重复请求行为。

详细约定见 `docs/05-api-and-data.md`。

## 开发前必须做什么

开始任务前，必须确认并输出：

1. 任务想解决的具体问题。
2. 本次包含和不包含的范围。
3. 涉及的模块、接口和 ADR。
4. 失败场景和兼容性影响。
5. 测试方式与可执行的验收命令。
6. 如果失败，如何回退当前改动。

任务单格式见 `docs/TASKS.md`。

## 标准开发循环

```text
读取文档
  -> 输出理解与计划
  -> 用户确认
  -> 创建功能分支
  -> 实现最小改动
  -> 编写或更新测试
  -> 本地构建与 Sanitizer
  -> 更新文档和 devlog
  -> 输出 Diff、结果和风险
  -> 用户验收
  -> PR/CI
  -> 合并
```

不得跳过用户确认，不得在没有测试的情况下宣称任务完成。

## 迭代准入条件

新增组件或复杂度必须至少满足一项：

- 当前实现无法满足已确认的功能需求。
- 压测显示明确的容量、延迟或资源瓶颈。
- 故障测试暴露现有设计无法恢复或会丢失数据。
- 安全、合规或一致性要求无法由现有方案满足。
- 演示和可验证性必须依赖的能力已经通过前序阶段形成基础。

仅“以后可能会用到”不构成引入 Kafka、etcd、多节点或 Kubernetes 的理由。

完整迭代路线见 `docs/02-roadmap.md`。

## 测试和完成定义

一个任务只有同时满足以下条件才算完成：

- 代码能够编译，无新增编译警告。
- 相关单元测试、集成测试或 E2E 测试通过。
- 涉及内存、并发时运行 ASan/TSan 或说明未运行原因。
- 接口、配置、数据迁移和错误路径有文档或测试。
- 没有提交构建产物、IDE 文件、密钥和本机路径。
- `docs/devlog.md` 已记录重要决策、问题和结果。
- 用户已审阅 Diff、运行验收命令并确认合并。

测试和可观测性标准见 `docs/04-quality-and-observability.md`。

## 协作纪律

- 项目所有者：需求确认、代码审阅、运行验收、合并和范围控制。
- 同一时间只能有一个执行者拥有代码写入权；写入权按任务授予，并记录在
  `docs/TASKS.md` 的当前任务中；交接写入权前必须先提交或回退未完成改动。
- 不承担写入权的协作者只输出任务单、验收标准、概念解释、报错分析和独立审查
  意见，不并发修改代码。
- 任何协作者的结论都必须经仓库文档和实际运行验证，不得直接作为实现依据。

## 输出格式

完成任务后按以下格式回复：

```text
任务：
结论：完成 / 部分完成 / 阻塞
修改文件：
设计说明：
测试命令与结果：
性能或安全影响：
未解决问题：
建议的下一步：
```

如果发现文档与实现冲突，停止编码并先报告冲突，不要自行选择方案。

## 当前阶段

当前为 `Phase 0：项目基线和环境建设`。

在完成设计审阅之前：

- 可以创建文档、目录、工程配置和验证脚本。
- 可以搭建最小 CMake/CI 骨架。
- 不得开始大规模业务实现。
