# Build outputs
build/
build-*/
cmake-build-*/

# CMake and compiler artifacts
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
compile_commands.json
*.o
*.obj
*.a
*.so
*.dylib
*.dll
*.exe

# IDE
.idea/
.vscode/
*.swp
*.swo

# CodeGraph local index
.codegraph/

# Local environment and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Runtime data and logs
logs/
data/
tmp/
*.log

# Test and benchmark outputs
coverage/
test-results/
perf.data*
flamegraph.svg

# OS
.DS_Store
Thumbs.db

# Node.js
node_modules/
dist/
.vite/

# Local maintenance scripts
# 以 scripts/_ 开头的临时或本机辅助脚本不纳入版本管理，避免被一次性提交
# 误同步到其他环境。需要长期保留的脚本请改用不带下划线前缀的名字。
scripts/_*.sh

# Database dumps and backups
# 数据库导出文件包含真实数据，绝不能进入版本库。
*.sql.gz
backup-*.sql
*_dump.sql
redis-dump-*.rdb

# 面试复习笔记
# 每个任务（版本迭代）一份，记录需要掌握的内容与答案，用于应对面试的项目深挖。
# 属于个人学习材料，不是项目交付物，不进入版本库。
interview-notes/

